import React from 'react';

import {CryptoSet, ThemeSet} from '../../components';

export const SideBar = () => {
    return(
        <div>
            <CryptoSet/>
            <ThemeSet/>
        </div>
    )
}
